pipeline {
    agent any

    tools {
        nodejs 'node14'
        jdk 'jdk17'
        maven 'maven3'
    }

    environment {
        AWS_DEFAULT_REGION = 'us-east-1'
        SONAR_SCANNER_HOME = tool 'sonarqube-scanner'
    }

    stages {

        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout Code') {
            steps {
                git url: 'https://github.com/staragile2016/Book-My-Show.git', branch: 'main'
                sh 'ls -la'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                with
